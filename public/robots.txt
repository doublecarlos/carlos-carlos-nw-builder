# The app is one client-rendered page, so there is nothing here to keep a crawler out of --
# except the OCR runtime, whose language model alone is tens of megabytes of binary a crawler
# would download and index nothing from. Those paths are served by the `tesseract-assets`
# plugin in vite.config.ts.
User-agent: *
Allow: /
Disallow: /tessdata/
Disallow: /tesseract/

# One page means a one-URL sitemap, but the line still names the canonical origin to a crawler
# that has not fetched the page yet. Same origin as the canonical link in index.html.
Sitemap: https://carloscarlosnwbuilder.pages.dev/sitemap.xml
